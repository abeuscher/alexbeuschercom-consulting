---
layout: base.njk
permalink: /
title: "Alex Beuscher"
description: "Alex Beuscher is a digital consultant." 
image: "/images/logo.png"
datePublished: "2024-01-01"
---
{% from "../macros/serviceitem.njk" import renderServiceItem %}
{% from "../macros/case-studies.njk" import renderCaseStudy %}

<article>
    <div class="content-wrapper padding-bottom">
        <div class="content">
            <section id="about">
                {{ content | safe }}
            </section>
            <section id="case-studies">
                <h1>Case Studies</h1>
                {% for study in sitedata.case_studies %}
                    {{ renderCaseStudy(study) }}
                {% endfor %}
            </section>
            <section id="services">
                <h1>Services</h1>
                {% for service in sitedata.services %}
                    {{ renderServiceItem(service) }}
                {% endfor %}
            </section>
            <!--
            <section id="packages">
                <h1>Packages</h1>
                {% for package in sitedata.packages %}
                    {{ renderServiceItem(package) }}
                {% endfor %}
            </section>
            //-->
            <section id="contact">
                <h1>Contact</h1>
                <p>lorem ipsum...</p>
            </section>
        </div>
    </div>
</article>