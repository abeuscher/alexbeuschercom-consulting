<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title | default("Alex Beuscher") }}</title>
        <meta name="description" content="{{ description }}">
        <meta property="og:title" content="{{ title }}">
        <meta property="og:description" content="{{ description }}">
        <meta property="og:image" content="{{ image | default('/images/default-image.jpg') }}">
        <meta property="og:url" content="{{permalink}}">
        <link href="https://fonts.googleapis.com/css2?family=Lato:wght@400;700&family=Source+Sans+Pro:wght@400;600&display=swap" rel="stylesheet">
        <!-- Styles and Scripts -->
        <link rel="stylesheet" href="/styles/theme.css">
        <script src="/scripts/app.js" defer></script>
        <!-- JSON-LD Structured Data -->
        <script type="application/ld+json">
            {
                "@context": "https://schema.org",
                "@type": "{{ pageType | default('WebPage') }}",
                "name": "{{ title }}",
                "datePublished": "{{ datePublished }}",
                "description": "{{ description }}",
                "url": "https://alexbeuscher.com{{ permalink }}",
                "image": "{{ metaImage | default('https://alexbeuscher.com/images/logo.jpg') }}",
                "author": {
                    "@type": "Person",
                    "name": "Alex Beuscher",
                    "jobTitle": "Digital Marketing & Web Consultant",
                    "url": "https://alexbeuscher.com"
                }
            }
        </script>
    </head>
    <body>
        {% include "header.njk" %}
        <div>{{ content | safe }}</div>
        {% include "footer.njk" %}
    </body>
</html>